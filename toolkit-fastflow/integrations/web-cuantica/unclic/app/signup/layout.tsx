export const metadata = {
  title: 'Crear cuenta',
  description: 'Crear cuenta UnClic. Continuar con Google o correo.',
};

export default function SignupLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
